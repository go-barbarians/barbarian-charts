apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-ignite-conf
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data: |-
{{ tpl (.Files.Glob "files/ignite/*").AsConfig . | indent 2 }}
